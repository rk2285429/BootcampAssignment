Find output of below code:
int main()
{ 
    int x = printf("ineuron");
    printf("%d",x);
    return 0;
}
OUTPUT - ineuron7, because printf() function return the number of character that are printed and also the 
characters